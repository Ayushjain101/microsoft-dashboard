# Dashboard auth
DASHBOARD_PASSWORD=your-password

# Encryption
FERNET_KEY=generate-with-python-c-from-cryptography.fernet-import-Fernet;print(Fernet.generate_key().decode())

# Database (localhost for native, container name for docker)
DATABASE_URL=postgresql+asyncpg://tenantadmin:dbpass@localhost:5432/tenants
DATABASE_URL_SYNC=postgresql://tenantadmin:dbpass@localhost:5432/tenants
DB_PASSWORD=dbpass

# Redis
REDIS_URL=redis://:redispass@localhost:6379/0
REDIS_PASSWORD=redispass

# CORS
CORS_ORIGINS=["https://decimastellarbolt.info"]
